<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weidudianshang">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".network.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.RegActivity" />
        <activity
            android:name=".activity.BottomBarActivity"
            android:label="@string/title_activity_bottom_bar" />
        <activity android:name=".activity.RxxpActivity" />
        <activity android:name=".activity.MissActivity" />
        <activity android:name=".activity.PzshActivity"></activity>
    </application>

</manifest>